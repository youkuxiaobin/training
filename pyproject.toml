[project]
name = "bpe-tokenizer"
version = "0.1.0"
description = "Byte-level BPE tokenizer utilities for pretraining experiments."
requires-python = ">=3.10"
dependencies = ["regex>=2024.0.0"]

[tool.setuptools.packages.find]
include = ["bpe_tokenizer*"]
